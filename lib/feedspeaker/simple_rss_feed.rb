# Simple ActiveRecord object containing the data from
# our RSS feed. It also contains a "read" flag allowing
# us to know which items are up for reading.

class SimpleRssFeed < ActiveRecord::Base
end